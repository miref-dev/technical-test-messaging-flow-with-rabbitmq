# ── MessageFlow API Service ──────────────────────────────────────────────────
PORT=3001

# RabbitMQ connection (must match worker-service topology)
RABBITMQ_URL=amqp://localhost

# Meta webhook verify token — set the same value in your Meta App Dashboard
WEBHOOK_VERIFY_TOKEN=messageflow-dev-token
